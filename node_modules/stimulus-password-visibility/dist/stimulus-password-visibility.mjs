import { Controller as i } from "@hotwired/stimulus";
class s extends i {
  connect() {
    this.hidden = this.inputTarget.type === "password", this.class = this.hasHiddenClass ? this.hiddenClass : "hidden";
  }
  toggle(t) {
    t.preventDefault(), this.inputTarget.type = this.hidden ? "text" : "password", this.hidden = !this.hidden, this.iconTargets.forEach((e) => e.classList.toggle(this.class));
  }
}
s.targets = ["input", "icon"];
s.classes = ["hidden"];
export {
  s as default
};
